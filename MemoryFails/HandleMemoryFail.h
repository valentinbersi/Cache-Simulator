#ifndef HANDLE_MEMORY_FAIL_H
#define HANDLE_MEMORY_FAIL_H

/**
 * Handles a memory fail, printing an error message and returning with code ENOMEM
 */
_Noreturn void handleMemoryFail(void);

#endif //HANDLE_MEMORY_FAIL_H
